SOLID Principle
S- SRP (single responsibility Principle)
one function should not do every thing it will create chaos 
what to does is split the work and create seperate function

O- OpenClose
it is like creating a function and using is again and again without changing the main function but using props from outside 

L- Liskov Substitution Principle
If you replace a component with another component, the app should still work perfectly.

I - interface segregation

D- Dependency Invdersion Principle


new things in reacts 19
---(useTransitions)supports for using async function in transition to handle pending stataces,errors ,forms

---contex as a provider
new hooks
-- useActionState
-- useFormStatus
-- useOptimistic
